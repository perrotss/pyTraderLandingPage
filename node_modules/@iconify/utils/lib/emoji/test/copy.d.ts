import { EmojiTestDataItem } from './parse.js';

/**
 * Item to copy
 */
interface EmojiSequenceToCopy {
    source: number[];
    sourceName: string;
    target: number[];
    targetName: string;
}
/**
 * Get sequences
 *
 * Returns map, where key is item to add, value is source
 */
declare function getEmojisSequencesToCopy(sequences: number[][], testData: EmojiTestDataItem[]): EmojiSequenceToCopy[];

export { getEmojisSequencesToCopy };
